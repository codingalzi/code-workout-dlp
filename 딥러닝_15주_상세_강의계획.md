# 딥러닝 15주 상세 강의계획

## 1. 강의 운영 방향

이 강의는 기존 강의노트인 `dlp2`를 전면적으로 새로 작성하기보다, 기존 내용을 **핵심 개념 중심으로 최대한 압축하여 수정·활용**하는 것을 기본 원칙으로 한다.

강의의 중심은 강의노트 자체가 아니라 **4개의 실전 프로젝트**에 둔다. 학생들은 필요한 개념을 먼저 짧게 학습하고, 이를 프로젝트에 바로 적용하며, 프로젝트 수행 과정에서 다시 필요한 개념을 배우는 방식으로 수업을 진행한다.

전체 수업의 기본 흐름은 다음과 같다.

> **핵심 개념 이해 → 프로젝트 적용 → 모델 평가 → 오류 분석 → 결과 해석과 판단**

딥러닝 프레임워크는 기존 강의노트의 **Keras** 코드를 기본으로 유지하되, 같은 개념을 구현한 **PyTorch 버전**도 함께 제공한다. 두 프레임워크를 별개의 내용으로 가르치지 않고, 같은 딥러닝 원리가 서로 다른 방식으로 표현된다는 점을 이해하도록 한다.

---

## 2. 강의노트 작성 기본 원칙

### 핵심만 남긴다

기존 `dlp2` 강의노트의 세부 설명과 API 사용법을 모두 유지하지 않는다. 각 장에서 학생이 반드시 이해해야 하는 핵심 개념을 중심으로 내용을 줄인다.

강의노트는 다음 질문을 기준으로 정리한다.

> **이 내용이 딥러닝의 핵심 원리를 이해하거나 프로젝트를 수행하는 데 반드시 필요한가?**

필요성이 낮은 세부 API, 반복 설명, 구현상의 변형, 지나치게 세밀한 수학적 전개는 축약하거나 참고 내용으로 돌린다.

### 개념을 먼저 설명한다

프레임워크 API보다 딥러닝의 공통 원리를 먼저 설명한다.

예를 들어 신경망 훈련은 다음 흐름을 먼저 이해하도록 한다.

> **입력 → 순전파 → 예측 → 손실 계산 → 그레이디언트 계산 → 가중치 갱신**

그 다음 같은 과정을 Keras와 PyTorch가 어떻게 표현하는지 비교한다.

### 수학은 이해를 위한 도구로 사용한다

수학적 유도 자체를 목표로 하지 않는다. 수학은 다음과 같은 질문에 답하기 위해 필요한 수준에서 사용한다.

- 신경망의 출력은 어떻게 계산되는가?
- 손실함수는 무엇을 측정하는가?
- 그레이디언트는 무엇을 의미하는가?
- 역전파는 왜 필요한가?
- attention은 입력 요소 사이의 관계를 어떻게 계산하는가?

### 코드는 암기 대상이 아니다

코드는 딥러닝 원리를 이해하고 프로젝트를 수행하기 위한 도구로 사용한다.

학생이 다음을 할 수 있도록 한다.

- 모델 코드의 전체 구조를 읽는다.
- 입력과 출력의 shape을 확인한다.
- 층, 활성화 함수, 손실함수, 옵티마이저의 역할을 설명한다.
- AI가 생성한 코드를 검토하고 필요한 부분을 수정한다.
- 훈련 결과와 평가 결과를 해석한다.

### 문장과 용어를 일관되게 사용한다

강의노트 전반에서 동일한 개념에는 동일한 용어를 사용한다.

예:

- **훈련**(training): 데이터를 이용하여 모델의 파라미터를 결정하는 구체적인 과정
- **학습**(learning): 데이터에서 관계나 표현을 알아내는 보다 넓은 의미
- **수치형 값**
- **범주형 값**

영어를 병기할 때는 다음 형식을 사용한다.

> **그레이디언트**(gradient)

한국어와 영어 전체를 하나의 굵은 글씨로 묶지 않는다.

---

## 3. 프로젝트 중심 수업 운영

전체 학기는 4개의 프로젝트를 중심으로 운영한다.

각 프로젝트는 **3주, 총 6개의 90분 수업**으로 구성한다.

각 프로젝트에서

- 3회는 핵심 개념 강의
- 3회는 프로젝트 실습

으로 진행한다.

기본 순서는 다음과 같다.

> **강의 1 → 강의 2 → 실습 1 → 강의 3 → 실습 2 → 실습 3**

이 순서를 사용하는 이유는 필요한 이론을 모두 먼저 설명한 뒤 프로젝트를 시작하지 않기 위해서다.

학생들은 기본 개념을 배운 뒤 바로 프로젝트를 시작하고, 프로젝트를 진행하면서 새롭게 필요한 개념을 다시 배우게 된다.

### 프로젝트 실습의 기본 흐름

각 프로젝트의 세 번의 실습은 대체로 다음과 같이 운영한다.

**실습 1**

- 문제 이해
- 데이터 이해
- 입력과 타깃 확인
- 기본 전처리
- 기준 모델 구성

**실습 2**

- 모델 훈련
- 학습 곡선 확인
- 여러 모델 또는 설정 비교
- 일반화 성능 평가
- 모델 개선

**실습 3**

- 오류 분석
- 최종 모델 선택
- 결과 해석
- 분석의 한계 확인
- AI가 생성한 코드와 해석 검토
- 프로젝트 결론 작성

프로젝트의 목표는 가장 높은 성능을 얻는 것이 아니다.

> **왜 그런 모델과 방법을 선택했는지 설명하고, 결과가 얼마나 믿을 만한지 판단할 수 있는 것**

을 중요하게 평가한다.

---

## 4. Keras와 PyTorch의 병행 원칙

### 하나의 개념을 두 가지 코드로 보여준다

Keras와 PyTorch를 각각 독립적인 학습 내용으로 다루지 않는다.

먼저 공통 개념을 설명하고, 그 개념이 두 프레임워크에서 어떻게 표현되는지 비교한다.

| 개념 | Keras | PyTorch |
|---|---|---|
| 완전연결층 | `Dense` | `nn.Linear` |
| 모델 구성 | `Sequential` 또는 Functional API | `nn.Module` |
| 순전파 | 모델 내부에서 처리 | `forward()` |
| 손실함수 | `compile(loss=...)` | loss function |
| 옵티마이저 | `compile(optimizer=...)` | `torch.optim` |
| 훈련 | `fit()` | training loop |
| 평가 | `evaluate()` | evaluation loop |
| 예측 | `predict()` | `model(x)` |
| 데이터 공급 | 배열 또는 Dataset | `Dataset`, `DataLoader` |

### P1에서 가장 자세히 비교한다

P1에서는 Keras와 PyTorch의 기본 구조를 비교적 자세히 다룬다.

특히 Keras의 `fit()`에 포함된 훈련 과정을 PyTorch의 training loop와 비교하여

> **예측 → 손실 계산 → 그레이디언트 계산 → 파라미터 갱신**

과정을 학생이 직접 확인할 수 있도록 한다.

### 이후에는 비교를 줄인다

P2 이후에는 같은 코드를 두 번 설명하지 않는다.

두 버전의 코드를 제공하되, 강의에서는 다음과 같이 차이가 중요한 부분만 설명한다.

- 층의 표현 방식
- 입력과 출력 shape
- 활성화 함수와 손실함수
- 옵티마이저 사용
- 훈련과 평가 과정
- 이미지 텐서의 차원 순서
- 모델 저장과 불러오기

### 프로젝트의 프레임워크 선택

프로젝트가 진행될수록 학생의 선택권을 넓힌다.

- **P1**: Keras와 PyTorch 두 버전을 모두 실행하고 비교
- **P2**: 두 버전을 제공하고 학생이 하나를 선택하여 프로젝트 수행
- **P3**: 기본 CNN은 두 버전을 제공하고 확장 분석은 선택한 프레임워크로 수행
- **P4**: Keras 또는 PyTorch 중 자유롭게 선택

평가는 어떤 프레임워크를 사용했는지가 아니라 **분석 설계, 모델 평가, 오류 분석, 해석과 판단**을 중심으로 한다.

---

# 5. 15주 상세 강의계획

## 1주 — 딥러닝 소개와 수업 운영

### 90분 1

- 인공지능, 머신러닝, 딥러닝의 관계
- 머신러닝과 딥러닝의 차이
- 신경망의 기본 아이디어
- 딥러닝이 필요한 문제
- 학기 전체 프로젝트 구조 소개

### 90분 2

- Keras와 PyTorch의 역할
- 같은 딥러닝 원리를 두 프레임워크로 표현한다는 관점
- Jupyter Notebook / Colab 실습 환경
- 프로젝트 수행 및 평가 방식
- 생성형 AI 활용 원칙

---

# P1 — 기본 신경망과 훈련

## 2주

### P1 강의 1 — 신경망의 기본 구조

- 입력층, 은닉층, 출력층
- 뉴런과 가중치
- 활성화 함수
- 입력에서 출력까지의 순전파
- 분류와 회귀에서 출력층이 달라지는 이유

### P1 강의 2 — 텐서와 첫 신경망

- 스칼라, 벡터, 행렬, 텐서
- 텐서의 shape과 axis
- batch의 의미
- 이미지와 표 형식 데이터의 텐서 표현
- Keras로 기본 MLP 구성
- 같은 모델의 PyTorch 표현

## 3주

### P1 실습 1 — 첫 딥러닝 프로젝트 시작

- 프로젝트 문제 확인
- 데이터 구조 확인
- 입력과 타깃 확인
- 데이터 전처리
- 기본 MLP 구성
- Keras와 PyTorch 모델 구조 비교

### P1 강의 3 — 신경망은 어떻게 훈련되는가?

- 모델 파라미터
- 예측값과 실제값
- 손실함수
- 그레이디언트의 의미
- 경사하강법
- 역전파
- 옵티마이저
- Keras `fit()`과 PyTorch training loop 비교

## 4주

### P1 실습 2 — 모델 훈련과 평가

- 모델 훈련
- 훈련 손실과 검증 손실
- 학습 곡선
- 훈련과 검증 성능 비교
- 기본 모델 개선

### P1 실습 3 — 오류 분석과 프로젝트 정리

- 잘못 예측된 샘플 확인
- 모델이 잘 작동하는 경우와 실패하는 경우
- Keras와 PyTorch 결과 비교
- 모델 선택
- 결과 해석과 한계
- P1 최종 정리

---

# P2 — 분류·회귀와 일반화

## 5주

### P2 강의 1 — 분류와 회귀

- 이진분류
- 다중분류
- 회귀
- 문제에 따른 출력층 선택
- 활성화 함수 선택
- 손실함수 선택
- 평가 지표 선택

### P2 강의 2 — 일반화와 모델 평가

- 훈련셋, 검증셋, 테스트셋
- 훈련 성능과 일반화 성능
- 과대적합과 과소적합
- 학습 곡선을 이용한 문제 진단
- 기준 모델과 모델 비교

## 6주

### P2 실습 1 — 실제 데이터 문제 시작

- 분석 문제 정의
- 데이터 이해
- 입력과 타깃 결정
- 데이터 준비
- 기준 신경망 모델 구축
- Keras 또는 PyTorch 선택

### P2 강의 3 — 일반화 성능 개선

- 모델 크기와 복잡도
- 규제
- 드롭아웃
- early stopping
- 학습률
- 모델 개선과 일반화의 관계

## 7주

### P2 실습 2 — 모델 개선과 비교

- 여러 모델 구조 비교
- 규제와 드롭아웃 적용
- 학습 곡선 비교
- 검증 성능을 이용한 모델 선택
- 과대적합 여부 확인

### P2 실습 3 — 최종 평가와 판단

- 테스트 데이터 최종 평가
- 오류 분석
- 최종 모델 선택
- 결과 해석
- 모델의 한계
- AI가 제안한 분석 또는 해석 검토
- P2 최종 정리

---

## 8주 — 중간시험

### 평가 범위

- 딥러닝과 신경망의 기본 개념
- 텐서와 데이터 표현
- 순전파
- 손실함수
- 그레이디언트와 역전파
- 경사하강법과 옵티마이저
- 분류와 회귀
- 훈련·검증·테스트
- 일반화
- 과대적합과 과소적합
- 규제와 드롭아웃

코드 API 암기보다는 개념 이해, 코드 해석, 결과 판단을 중심으로 평가한다.

---

# P3 — 합성곱 신경망

## 9주

### P3 강의 1 — CNN의 기본 아이디어

- 완전연결 신경망과 이미지 데이터
- 합성곱의 필요성
- 필터와 커널
- 특성맵
- 지역적 관계와 가중치 공유

### P3 강의 2 — CNN의 구성 요소

- `Conv2D` / `nn.Conv2d`
- stride
- padding
- pooling
- CNN의 입력과 출력 shape
- Keras와 PyTorch 이미지 텐서 차원 순서의 차이

## 10주

### P3 실습 1 — 이미지 프로젝트 시작

- 이미지 데이터 구조 확인
- 데이터 전처리
- 기본 CNN 구성
- 모델 훈련
- 학습 곡선 확인
- 기본 성능 평가

### P3 강의 3 — CNN 성능 개선

- 데이터 증식
- 사전훈련 모델
- 전이 학습
- 특성 추출
- fine-tuning의 기본 아이디어
- 언제 직접 CNN을 훈련하고 언제 사전훈련 모델을 활용할 것인가

## 11주

### P3 실습 2 — CNN 모델 비교

- 기본 CNN
- 데이터 증식 적용 모델
- 전이 학습 모델
- 훈련·검증 성능 비교
- 모델 선택

### P3 실습 3 — 이미지 오류 분석과 판단

- 오분류 이미지 확인
- 클래스별 성능 확인
- 데이터 문제와 모델 문제 구분
- 최종 모델 평가
- 결과 해석과 한계
- P3 최종 정리

---

# P4 — Attention과 Transformer

## 12주

### P4 강의 1 — Attention

- Attention이 필요한 이유
- 입력 요소 사이의 관계
- Query, Key, Value
- attention score
- weighted sum
- self-attention의 기본 아이디어

### P4 강의 2 — Transformer

- Transformer의 전체 구조
- self-attention
- multi-head attention
- positional encoding
- feed-forward network
- residual connection의 기본 역할
- Transformer가 RNN 없이 입력을 처리하는 방식

## 13주

### P4 실습 1 — Transformer 프로젝트 시작

- 프로젝트 데이터 이해
- 입력 데이터 표현
- 토큰과 임베딩
- 기본 attention 또는 Transformer 모델 구성
- Keras와 PyTorch 구현 구조 비교

### P4 강의 3 — Transformer 활용

- Transformer 기반 모델의 활용
- 사전훈련 모델의 기본 아이디어
- fine-tuning
- 모델 출력의 해석
- Transformer 모델의 평가와 오류 분석
- 모델 크기와 계산 비용에 대한 기본 이해

## 14주

### P4 실습 2 — 모델 훈련과 비교

- 기본 모델과 Transformer 모델 비교
- 훈련 및 검증
- 하이퍼파라미터 또는 모델 설정 비교
- 일반화 성능 확인
- 오류 유형 분석

### P4 실습 3 — 최종 판단

- 최종 모델 선택
- 오류 분석
- 분석 결과 해석
- 모델의 한계
- AI가 생성한 코드와 설명 검토
- Keras/PyTorch 구현 차이 정리
- P4 최종 정리

---

## 15주 — 종합 정리와 기말시험

### 90분 1 — 딥러닝 전체 구조 정리

학기 동안 배운 모델을 하나의 흐름으로 연결한다.

> **MLP → CNN → Attention → Transformer**

다음 질문을 중심으로 정리한다.

- 각 모델은 어떤 종류의 관계를 학습하는가?
- 데이터의 형태에 따라 어떤 모델이 적절한가?
- 모델의 훈련과 일반화는 어떻게 평가하는가?
- 모델이 실패했을 때 무엇을 확인해야 하는가?
- 사전훈련 모델을 언제 활용하는 것이 좋은가?
- Keras와 PyTorch의 차이는 무엇이고 공통 원리는 무엇인가?

### 90분 2 — 기말시험

개념 이해, 코드 해석, 모델 선택, 평가 결과 해석, 오류 분석, 일반화와 한계 판단을 중심으로 평가한다.

---

# 6. 프로젝트 평가의 기본 원칙

프로젝트는 코드의 길이나 모델의 복잡도를 중심으로 평가하지 않는다.

다음 요소를 중심으로 평가한다.

1. **문제 이해**
   - 무엇을 해결하려는지 명확하게 정의했는가?

2. **데이터 이해**
   - 입력과 타깃, 데이터의 구조와 한계를 이해했는가?

3. **모델과 방법 선택**
   - 모델 구조, 손실함수, 평가 지표 등의 선택에 근거가 있는가?

4. **모델 평가**
   - 훈련 성능과 일반화 성능을 적절히 구분했는가?

5. **오류 분석**
   - 모델이 언제 잘 작동하고 언제 실패하는지 확인했는가?

6. **해석과 판단**
   - 결과로 말할 수 있는 것과 말할 수 없는 것을 구분했는가?

7. **AI 활용 검증**
   - AI가 생성한 코드나 설명을 그대로 받아들이지 않고 검토했는가?

---

# 7. 생성형 AI 활용 원칙

생성형 AI의 사용을 제한하기보다 **적절하게 활용하고 검증하는 능력**을 기르는 것을 목표로 한다.

AI는 다음 작업에 활용할 수 있다.

- Keras/PyTorch 코드 생성
- 코드 변환
- 데이터 전처리 코드 제안
- 모델 구조 제안
- 오류 수정
- 그래프 작성
- 결과 설명 초안 작성

그러나 다음 판단은 학생이 직접 수행해야 한다.

- 어떤 문제를 해결할 것인가?
- 입력과 타깃을 어떻게 정의할 것인가?
- 어떤 모델을 선택할 것인가?
- 어떤 손실함수와 평가 지표가 적절한가?
- 훈련·검증·테스트 데이터가 적절히 구분되었는가?
- 높은 성능을 그대로 믿어도 되는가?
- 모델은 어떤 데이터에서 실패하는가?
- 결과에서 어떤 결론을 내릴 수 있는가?
- AI가 작성한 코드와 설명이 타당한가?

> **AI 시대의 딥러닝 교육에서 중요한 것은 코드를 직접 모두 작성하는 능력보다, 모델과 분석 과정을 이해하고 검증하며 판단하는 능력이다.**

---

# 8. 강의의 전체 목표

이 강의는 학생들이 다양한 딥러닝 알고리즘을 많이 아는 것을 목표로 하지 않는다.

핵심 목표는 다음 세 단계로 요약한다.

> **이해 → 활용 → 판단**

- 딥러닝 모델의 핵심 원리를 **이해한다**.
- Keras와 PyTorch를 이용해 실제 문제에 **활용한다**.
- 훈련 결과와 일반화 성능, 오류와 한계를 분석하여 모델의 타당성을 **판단한다**.

4개의 프로젝트를 통해 이 과정을 반복적으로 경험하도록 한다.
